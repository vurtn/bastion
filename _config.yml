title: Bastion
email: vurtn@protonmail.com
description: #>- # this means to ignore newlines until "baseurl:"
  #Write an awesome description for your new site here. You can edit this
  #line in _config.yml. It will appear in your document head meta (for
  #Google search results) and in your feed.xml site description.
baseurl: "/" # the subpath of your site, e.g. /blog
url: "https://vurtn.github.io" # the base hostname & protocol for your site, e.g. http://example.com
github_username:  vurtn

plugins:
  - jekyll-feed
  - jekyll-paginate

sass:
  sass_dir: _sass
  style: compressed

paginate: 6
paginate_path: "/bastion/page:num/"

defaults:
  - scope:
      path: "assets/img"
    values:
      image: true
